---
/**
 * `/` — home (default locale: Chinese).
 * Route logic lives in the view so both locales share one implementation.
 */
import HomePage from '@/views/HomePage.astro';
---

<HomePage locale="zh" />
